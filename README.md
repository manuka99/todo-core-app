# todo-core-app

Full-stack TODO application (React + Express + MongoDB). Monorepo with npm workspaces.

## Quick start

```bash
npm install
npm run dev:server   # API (default port 5000)
npm run dev:client   # Vite dev server
```

See `client/README.md` and `server/README.md` for details.
